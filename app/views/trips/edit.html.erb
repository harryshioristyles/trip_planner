<h1>Trips#edit</h1>
	<div>
		<div>
			<%= form_for @trip do |f| %>
				<div class="field">
					<%= image_tag(@trip.trip_image) if @trip.trip_image.attached? %>
				    <%= f.file_field :trip_image %><br>
	            </div>
				<div class="field">
				    <%= f.label :trip_title, "Trip Title" %>
				    <%= f.text_field :trip_title %>
	            </div>
				<div class="field">
				    <%= f.label :trip_details, "Trip Details" %>
				    <%= f.text_field :trip_details %>
	            </div>
	            <%= f.submit :UPDATE %><br>
    			<%= link_to "Delete", trip_path(@trip), method: :delete, "data-confirm" => "Are you sure?" %><br>
	        <% end %>
	    </div>
			<% @trip.lists.each do |list| %>
					Day.<%= list.day_index %>
					date:<%= list.date %><br>
					begin:<%= list.begin %>
					end:<%= list.end %><br>
					list_title:<%= list.list_title %><br>
					list_details:<%= list.list_details %><br>
					City:<%= list.city.city_name %><br>
					Activity:<%= list.activity.activity_name %><br>
			<% end %>
    </div>