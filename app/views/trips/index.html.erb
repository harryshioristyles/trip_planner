<h1>Trips#index</h1>
			<% @trips.each do |trip| %>
				    "Trip Title"<br>
				    <%= trip.trip_title %><br>
				    "Trip Details"<br>
				    <%= trip.trip_details %><br>
					<%= link_to "Show", trip_path(trip) %><br>
					<%= link_to "Edit", edit_trip_path(trip) %><br>
			<% end %>